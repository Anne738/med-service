@model med_service.Models.Doctor

@{
    ViewData["Title"] = "Создание";
}

<h1>Создание</h1>

<h4>Врач</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group mb-3">
                <label asp-for="UserId" class="control-label">Пользователь</label>
                <select asp-for="UserId" class="form-control" asp-items="ViewBag.UserId"></select>
            </div>
            <div class="form-group mb-3">
                <label asp-for="HospitalId" class="control-label">Больница</label>
                <select asp-for="HospitalId" class="form-control" asp-items="ViewBag.HospitalId"></select>
            </div>
            <div class="form-group mb-3">
                <label asp-for="SpecializationId" class="control-label">Специализация</label>
                <select asp-for="SpecializationId" class="form-control" asp-items="ViewBag.SpecializationId"></select>
            </div>
            <div class="form-group mb-3">
                <label asp-for="ExperienceYears" class="control-label">Опыт работы (годы)</label>
                <input asp-for="ExperienceYears" class="form-control" />
                <span asp-validation-for="ExperienceYears" class="text-danger"></span>
            </div>
            <div class="form-group mt-3">
                <input type="submit" value="Создать" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Вернуться к списку</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
