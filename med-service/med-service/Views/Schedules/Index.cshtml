@model IEnumerable<med_service.Models.Schedule>

@{
    ViewData["Title"] = "Розклади";
}

<h1>Розклади</h1>

<p>
    <a asp-action="Create" class="btn btn-primary">Створити новий розклад</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                День тижня
            </th>
            <th>
                Лікар
            </th>
            <th>
                Робочі години
            </th>
            <th>Дії</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Day)
                </td>
                <td>
                    @if (item.Doctor?.User != null)
                    {
                        @($"{item.Doctor.User.LastName} {item.Doctor.User.FirstName}")
                    }
                    else
                    {
                        @Html.DisplayFor(modelItem => item.Doctor.Id)
                    }
                </td>
                <td>
                    @item.WorkingHours
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-warning">Редагувати</a>
                    <a asp-action="Details" asp-route-id="@item.Id" class="btn btn-sm btn-info">Деталі</a>
                    <a asp-action="Delete" asp-route-id="@item.Id" class="btn btn-sm btn-danger">Видалити</a>
                </td>
            </tr>
        }
    </tbody>
</table>
